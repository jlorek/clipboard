<div id="<%= @id %>">
    <div>Who is watching?</div>
    <%= for other <- @connected_users do %>
    <%# this List.last thing should be done another way... %>
    <span><%= other.name %><%= if other.uuid == @user.uuid do %> (it's you)<% end %><%= if other != List.last(@connected_users) do %> · <% end %></span>
    <% end %>
</div>
